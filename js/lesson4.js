"use strict"

function getСomparison(a, b) {
    let numOne = a;
    let numTwo = b;

    if (numOne < numTwo) {
        let result = -1;
        console.log(`${result}`);
    } else if (numOne > numTwo) {
        let result = 1;
        console.log(`${result}`);
    } else if (numOne == numTwo) {
        let result = 0;
        console.log(`${result}`);
    };
};








